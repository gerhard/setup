#!/bin/bash

installed_formulae="$(brew list)"

pushd "${BASE_PATH}/homebrew/formulae" >&-
  for formula in *
  do
    formula_options="$(cat "$formula")"
    grep "$formula" >&- <<< "$installed_formulae" || brew install "$formula" "$formula_options"
  done
popd >&-
